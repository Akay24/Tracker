export const SYSTEM_DESIGN_SECTIONS = [
  { key: "requirements",      label: "Requirements",       prompt: "Functional + Non-functional. Clarify scale, SLA, consistency needs." },
  { key: "api_design",        label: "API Design",         prompt: "REST/gRPC endpoints, request/response shapes, pagination, idempotency keys." },
  { key: "data_model",        label: "Data Model",         prompt: "Schema, primary keys, indexes (B-tree/covering/partial), sharding key choice." },
  { key: "scaling",           label: "Scaling Strategy",   prompt: "Read/write ratio, horizontal scaling, DB sharding or federation approach." },
  { key: "caching",           label: "Caching",            prompt: "What to cache, where (CDN/app/DB), eviction policy, invalidation strategy." },
  { key: "tradeoffs",         label: "Trade-offs",         prompt: "CAP theorem choice, consistency vs availability, cost vs latency." },
  { key: "bottlenecks",       label: "Bottlenecks",        prompt: "Single points of failure, hot partitions, thundering herd, slow queries." },
  { key: "failure_handling",  label: "Failure Scenarios",  prompt: "What happens when cache is down, DB failover, queue backlog, retry storms." },
];

export const SYSTEMS = [
  {
    id: "tinyurl",
    name: "TinyURL",
    icon: "🔗",
    difficulty: "Medium",
    tags: ["Hashing", "Caching", "Scale"],
    keyInsights: [
      "Base62 encoding of auto-increment ID vs MD5 hash truncation",
      "Cache-aside with Redis for redirect hot path (target <5ms p99)",
      "Consistent hashing for multi-region redirect servers",
      "Analytics via async queue to avoid write amplification on hot URLs",
    ],
    estimations: "500M URLs, 100:1 read/write ratio, 10K redirects/sec peak",
    apiSketch: "POST /shorten · GET /:code (301/302) · DELETE /:code · GET /analytics/:code",
    dataModelSketch: "urls(id PK, short_code UNIQUE, long_url, user_id FK, created_at, expires_at) · clicks(id, short_code, clicked_at, ip_hash, country)",
    scalingStory: "Single DB → Redis cache-aside → Read replicas → Sharding by short_code hash → Multi-region with eventual consistency",
    failureScenarios: "Redis down → DB fallback, hot URL → local cache tier, DB write failure → retry with idempotency key",
  },
  {
    id: "rate-limiter",
    name: "Rate Limiter",
    icon: "🚦",
    difficulty: "Medium",
    tags: ["Algorithms", "Distributed", "Redis"],
    keyInsights: [
      "Token bucket (bursty traffic) vs sliding window log (precise) vs fixed window (simple, edge case at boundary)",
      "Sliding window counter: blend of fixed window with weight from previous window",
      "Redis INCR + EXPIRE for distributed rate limiting; EVALSHA for atomic Lua scripts",
      "Rate limit headers: X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset",
    ],
    estimations: "10K RPM per user, 1M users, Redis must handle 100K ops/sec",
    apiSketch: "Middleware: check-rate-limit(userId, endpoint) → allow/deny + headers",
    dataModelSketch: "Redis: key=rl:{userId}:{window} val=count EX=window_seconds · Lua script for atomic check+incr",
    scalingStory: "Single Redis → Redis Cluster with consistent hashing → local in-memory + async sync for extreme scale",
    failureScenarios: "Redis down → fail-open vs fail-closed choice (business decision), race condition → Lua atomic script",
  },
  {
    id: "whatsapp",
    name: "WhatsApp",
    icon: "💬",
    difficulty: "Hard",
    tags: ["Messaging", "WebSockets", "Scale"],
    keyInsights: [
      "WebSocket for persistent connection; connection server keeps session map userId→socketId",
      "Message fan-out: 1:1 via direct lookup, group via member list expansion at send time",
      "Message queue (Kafka) between sender and delivery service for durability",
      "End-to-end encryption: Signal protocol; server stores only ciphertext",
      "Online presence: heartbeat every 30s, last-seen updated in Redis TTL",
    ],
    estimations: "2B users, 100B messages/day, 1M concurrent WebSocket connections per region",
    apiSketch: "sendMessage(to, content, type) · getMessages(conversationId, cursor) · markDelivered(msgId) · markRead(msgId)",
    dataModelSketch: "messages(id, conversation_id, sender_id, content_encrypted, status, created_at) · conversations(id, type, members[]) · user_sessions(user_id, socket_id, server_id)",
    scalingStory: "WebSocket servers behind NLB → Kafka for message durability → Cassandra for message storage (write-heavy, time-series) → Redis for presence",
    failureScenarios: "Connection server crash → reconnect + replay unacked messages, Kafka consumer lag → back-pressure, multi-device delivery via notification service",
  },
  {
    id: "news-feed",
    name: "News Feed",
    icon: "📰",
    difficulty: "Hard",
    tags: ["Fan-out", "Ranking", "Caching"],
    keyInsights: [
      "Fan-out on write (push) vs fan-out on read (pull) vs hybrid approach",
      "Hybrid: push for regular users, pull for celebrity accounts (high follower count)",
      "Feed ranking: ML model score = recency + engagement + relationship strength",
      "Pagination via cursor (post_id + score) not offset to avoid drift",
      "Pre-computed feed cache in Redis sorted set (score = rank_score)",
    ],
    estimations: "1B users, 500M DAU, avg 200 followers, 100M new posts/day, feed read 10K QPS",
    apiSketch: "GET /feed?cursor=&limit= · POST /post · POST /like · POST /follow",
    dataModelSketch: "posts(id, author_id, content, media_url, created_at) · follows(follower_id, followee_id, created_at) · feed_cache(user_id → sorted_set of post_ids)",
    scalingStory: "Single feed table → materialized feed per user in Redis → async fan-out workers via queue → CDN for media",
    failureScenarios: "Fan-out worker backlog → degrade to pull mode, Redis eviction → rebuild feed on miss, celebrity post → rate-limit fan-out workers",
  },
  {
    id: "file-storage",
    name: "File Storage (Dropbox)",
    icon: "📦",
    difficulty: "Hard",
    tags: ["Chunking", "S3", "Sync"],
    keyInsights: [
      "Chunked upload: split file into 4MB chunks, upload in parallel, resume on failure",
      "Delta sync: only upload changed chunks (content-addressable via SHA-256)",
      "Metadata server separate from blob storage; S3-compatible object store for blobs",
      "Long-polling or WebSocket for real-time sync notifications",
      "Deduplication at chunk level across users (same SHA-256 → same S3 object)",
    ],
    estimations: "500M users, 50M DAU, avg 10GB storage/user, 10M file uploads/day",
    apiSketch: "POST /upload/init · PUT /upload/chunk · POST /upload/complete · GET /files · GET /download/:fileId",
    dataModelSketch: "files(id, user_id, name, size, status, created_at) · chunks(id, file_id, chunk_index, sha256, s3_key) · sync_events(id, user_id, file_id, event_type, device_id)",
    scalingStory: "Single server → S3 for blobs + PostgreSQL for metadata → CDN for downloads → separate sync service with WebSocket",
    failureScenarios: "Upload interrupted → resume via chunk manifest, S3 outage → cross-region replication, metadata DB failure → read from replica",
  },
];
